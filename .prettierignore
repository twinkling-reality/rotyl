dist
node_modules
.scratch
public/fonts
pnpm-lock.yaml
# Generated by tools/edgetam-export: a Python venv, model graphs and a fixture.
tools/edgetam-export/venv
tools/edgetam-export/onnx
tools/edgetam-export/fixture
tools/video-bench/clips
tools/style-bench/clips
tools/style-bench/out
tools/style-bench/real
# Committed, but written by the harness rather than by a person.
tools/style-bench/figures
tools/video-bench/results.json
# And where the sound goes, whose codec list a formatter collapses onto one
# line: a re-take would then fail format:check for having been re-taken.
tools/video-bench/results-interleave.json
# The stage table, and the probes test/memory-bank.test.ts drives the
# TypeScript over. The fixture is written on one line on purpose: it is forty
# thousand numbers, and a formatter would make every re-take a forty-thousand
# line diff of values nobody reads by eye.
tools/edgetam-export/host.json
tools/edgetam-export/host-fixture.json
# And the tracking sweep, which wraps its frame arrays differently at thirty
# frames than at ten. Formatting it by hand would only fail again the next time
# somebody re-takes it, which is the trap the entries above avoid.
tools/edgetam-export/results.json
# The occlusion clip's ground truth, copied out of tools/edgetam-export/fixture
# by the command in e2e/fixtures/README.md. Formatted here it would stop being
# the copy that command makes, which is the one thing the command has to be.
e2e/fixtures/occlusion.json
